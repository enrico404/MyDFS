Resistituisce i file nella directory corrente

Uso:
	ls [-a, -d]

Opzioni:
	-a -> per vedere anche i meta dati dei file
	-d -> per calcolare anche la dimensione delle directory

Esempi d'uso:
    ls -a
    ls -d
    ls -a -d
