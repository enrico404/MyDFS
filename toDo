7) balancing dei server
8) bindare freccia su a comando precedente (non possibile in una applicazione console, necessario portare tutto in gui)
1) possibilità di configurare  una directory condivisa differente per i server (durante la fase di installazione)
2) GUI (??)
